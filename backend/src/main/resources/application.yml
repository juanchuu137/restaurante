server:
  port: ${PORT:8080}
spring:
  application:
    name: RestauranteApi
  datasource:
    # Supabase (Postgres) connection
    url: ${SUPABASE_DB_URL:jdbc:postgresql://aws-1-us-east-2.pooler.supabase.com:6543/postgres}
    username: ${SUPABASE_DB_USER:postgres.pdvcqrvgqlvondifioas}
    password: ${SUPABASE_DB_PASSWORD:LMkh684WymLV3t&}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

app:
  jwt:
    # Secret de al menos 32 caracteres para HMAC-SHA256.
    # En producción, inyectar desde variable de entorno: APP_JWT_SECRET
    secret: ${APP_JWT_SECRET:mi-secret-local-super-seguro-1234567890abcdef}
    expiration-hours: ${APP_JWT_EXPIRATION_HOURS:24}

openfoodfacts:
  api-url: https://world.openfoodfacts.org
